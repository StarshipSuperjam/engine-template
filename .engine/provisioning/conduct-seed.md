---
codes: []
---

<!-- The maintainer's seed for a generated project's .engine/conduct/operator.md. First-run setup copies
this file into that project's operator override, so a stance authored once here rides every project made
from this template. It ships EMPTY (codes: []) — a third party generating from the public template gets the
engine's defaults plus an empty override they fill themselves. Add codes here (the operator.md shape: a
`codes` entry plus a matching `## <title>` section) to seed your own stance, or use /engine-conduct's
promote-to-seed step. This file is the maintainer's own config: an engine update never overwrites it. -->
