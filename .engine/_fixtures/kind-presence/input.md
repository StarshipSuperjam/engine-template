## Purpose

This file is a deliberately-broken example: it is missing the required `Detail` section, so the
presence check must catch it. (It exists only to prove that check still bites.)

## Scope

It has `Purpose` and `Scope` filled in, but no `Detail` section at all — the omission the
presence check is supposed to flag.
